/**
 * Skills section copy.
 *
 * Kept apart from `skills.js` so that file stays a pure inventory of
 * technologies. Section headings change for editorial reasons; the stack
 * changes for technical ones. Different reasons to edit, different files.
 */

export const SKILLS_META = {
  badge: 'Skills',

  /** Same line model as the hero and about headings: `accent` marks the one
   *  line rendered in italic serif with a gradient fill. */
  headline: [{ text: 'The tools behind' }, { text: 'the work', accent: true }],

  intro:
    'I work across the stack with a bias toward the interface. Below is what I reach for, grouped by where it sits — and what I am still learning, listed honestly.',
}
