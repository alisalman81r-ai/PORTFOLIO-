/**
 * Projects section.
 *
 * A folder because the section has five independently meaningful parts: the
 * orchestrator, the filter bar, one showcase row, the preview media, and the
 * case-study modal.
 *
 * Only `Projects` is public. Anything that turned out to be reusable beyond
 * this section moved to `@/components/ui` instead — which is where `Modal`
 * came from, and `useFocusTrap` in `@/hooks` with it.
 */

export { Projects } from './Projects'
