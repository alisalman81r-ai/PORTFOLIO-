/**
 * Contact section.
 *
 * A folder because the form and the details panel are independently meaningful.
 * Only `Contact` is public — the reusable parts moved to `@/components/ui`
 * (`FormField`) and `@/hooks` (`useForm`) while building it, since neither is
 * specific to this section.
 */

export { Contact } from './Contact'
