/**
 * Blog / insights section.
 *
 * A folder because the card is independently meaningful and memoised on its
 * own. Only `Blog` is public — and it returns null below two posts, so the page
 * composes it unconditionally.
 */

export { Blog } from './Blog'
