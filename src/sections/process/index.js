/**
 * Work process section.
 *
 * A folder because the step is independently meaningful — it owns the hover
 * spotlight and its own entrance. Only `Process` is public.
 */

export { Process } from './Process'
