/**
 * Testimonials section.
 *
 * A folder because the slide is independently meaningful and memoised on its
 * own. Only `Testimonials` is public — and it returns null when there are fewer
 * than two entries, so the page composes it unconditionally.
 */

export { Testimonials } from './Testimonials'
