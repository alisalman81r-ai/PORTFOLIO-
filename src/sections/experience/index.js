/**
 * Experience section.
 *
 * A folder because the entry is independently meaningful. Only `Experience` is
 * public — the scrubbed rail it uses lives in `@/components/ui` as
 * `TimelineRail`, since it is not specific to this section.
 */

export { Experience } from './Experience'
