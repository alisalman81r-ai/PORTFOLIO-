/**
 * Skills section.
 *
 * A folder because the section has four independently meaningful parts: the
 * category rail (a full ARIA tablist), the details panel, the card, and the
 * background decoration.
 *
 * Only `Skills` is public. The rest are implementation details — anything that
 * turned out to be reusable beyond this section moved to `@/components/ui`
 * instead, which is where `TechIcon` came from while building it.
 */

export { Skills } from './Skills'
